# Copy to .env and fill in. .env is gitignored.
ANTHROPIC_API_KEY=        # Claude — scriptwriter step
ELEVENLABS_API_KEY=       # ElevenLabs — Odie voice TTS
ODIE_VOICE_ID=            # ElevenLabs cloned voice id for Odie
HIGGSFIELD_API_KEY=       # Higgsfield REST — key half of the pair
HIGGSFIELD_API_SECRET=    # Higgsfield REST — secret half (Authorization: Key KEY:SECRET)
PORT=3000
DAILY_CLIP_CAP=50         # simple cost guardrail
